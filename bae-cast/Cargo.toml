[package]
name = "bae-cast"
version = "0.1.0"
edition = "2021"
license = "LicenseRef-UNLICENSED"
publish = false

[dependencies]
bae-core = { path = "../bae-core" }
# The ephemeral Subsonic server the cast receiver fetches audio from: binds the
# bae-subsonic router on a random LAN port, authenticated by a per-session
# credential. This is why the controller is its own crate rather than part of
# bae-core, which must not depend on bae-subsonic (see bae-core/src/renderer).
bae-subsonic = { path = "../bae-subsonic" }
tokio = { version = "1", features = ["rt", "sync", "net"] }
tracing = { workspace = true }
# axum 0.8 matches the router bae-subsonic returns.
axum = { version = "0.8", default-features = false, features = ["http1", "tokio"] }
tokio-util = "0.7"
# The salted-token the minted media URLs carry (md5(password + salt)), matching
# bae-subsonic's auth check. Keyed `md5` (its lib name) so cargo-machete resolves
# `use md5::…`; the crate is `md-5` but exposes `md5`.
md5 = { package = "md-5", version = "0.10" }
hex = "0.4"
rand = "0.9"
# The LAN IP the receiver reaches this device at (the ephemeral server binds
# 0.0.0.0, but the URL must name a routable address).
local-ip-address = "0.6"

[dev-dependencies]
bae-test-support = { path = "../bae-test-support" }
bae-core = { path = "../bae-core", features = ["test-utils"] }
tempfile = "3"
reqwest = { workspace = true, features = ["rustls"] }

[lints]
workspace = true
