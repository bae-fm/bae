---
title: 自动化
---

## MCP 服务器

在 Settings、Automation 中打开 MCP 服务器。它通过本地端口上的 HTTP 提供 Model Context Protocol（默认 47777），因此 Claude 这类 MCP 客户端可以浏览你的资料库、查找发行并对其执行操作。访问需要 bearer token：从同一个设置面板复制；想切断所有持有旧 token 的访问时，也在那里轮换它。

服务器默认关闭，并且只在应用运行时运行。
