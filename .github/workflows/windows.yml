name: Windows build

# Builds the native Windows stack for x86_64-pc-windows-msvc: bae-core →
# bae-bridge (uniffi metadata + bae_bridge.dll) → generated C# bindings, plus
# the WinUI 3 app compile check. The macOS host can't cross-compile the native
# deps, so this is the verification loop.
# The native deps are all prebuilt releases build.rs/CI fetch — no compiling
# here: ffmpeg via bae-ffmpeg, libdiscid via MusicBrainz's prebuilt.

on:
  workflow_call:

jobs:
  build:
    runs-on: windows-latest
    env:
      FFMPEG_VERSION: v8.1.2-bae5
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Rust (pinned toolchain)
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95.0"
          targets: x86_64-pc-windows-msvc
          components: clippy

      - name: Install sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Set LIBCLANG_PATH (LLVM pre-installed on windows-latest)
        shell: pwsh
        run: |
          echo "LIBCLANG_PATH=C:\Program Files\LLVM\bin" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Fetch libdiscid (MusicBrainz Windows binary)
        shell: pwsh
        run: |
          # The `discid` crate (import disc-id matching) links discid.lib and has
          # no bindgen/headers. libdiscid isn't in vcpkg, so use MusicBrainz's
          # prebuilt x64 binary; the linker finds discid.lib via LIB, the DLL via
          # PATH.
          $url = "https://github.com/metabrainz/libdiscid/releases/download/v0.6.5/libdiscid-0.6.5-win.zip"
          curl.exe -L $url -o libdiscid.zip
          Expand-Archive -Path libdiscid.zip -DestinationPath "$PWD\libdiscid" -Force
          $x64 = "$PWD\libdiscid\libdiscid-0.6.5-win\x64"
          echo "LIB=$x64;$env:LIB" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          echo "$x64" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8

      - name: Fetch bae-ffmpeg (Windows x86_64)
        shell: pwsh
        run: |
          $dist = "$PWD\bae-ffmpeg\dist"
          New-Item -ItemType Directory -Force -Path $dist | Out-Null
          $url = "https://github.com/bae-fm/bae-ffmpeg/releases/download/$env:FFMPEG_VERSION/ffmpeg-windows-x86_64.zip"
          curl.exe -L $url -o ffmpeg.zip
          Expand-Archive -Path ffmpeg.zip -DestinationPath $dist -Force
          # The Windows build ships the .lib import libs in bin/ next to the DLLs,
          # with no lib/ or pkgconfig/; ffmpeg-sys-next expects the libs under lib/.
          New-Item -ItemType Directory -Force -Path "$dist\lib" | Out-Null
          Copy-Item "$dist\bin\*.lib" "$dist\lib\" -Force
          echo "FFMPEG_DIR=$dist" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          # Point bindgen (ffmpeg-sys) at the ffmpeg headers, overriding the macOS
          # Homebrew include args .cargo/config.toml sets with force=false.
          echo "BINDGEN_EXTRA_CLANG_ARGS=-I$dist\include" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          echo "$dist\bin" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8

      - name: Provision ffmpeg's mingw runtime deps (msys2)
        shell: pwsh
        run: |
          # bae-ffmpeg's Windows DLLs are a mingw build and depend on the mingw
          # runtime (libiconv/lame/zlib/winpthread) without bundling it.
          C:\msys64\usr\bin\bash.exe -lc "pacman -S --noconfirm --needed mingw-w64-x86_64-lame mingw-w64-x86_64-opus mingw-w64-x86_64-libiconv mingw-w64-x86_64-zlib mingw-w64-x86_64-gcc-libs"
          echo "C:\msys64\mingw64\bin" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8

      - name: Check Rust formatting
        run: cargo fmt --all -- --check

      - name: Install .NET 8 SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "8.0.x"

      - name: Check C# formatting
        # dotnet format uses .editorconfig rules. --verify-no-changes exits non-zero
        # if any file would be reformatted, failing the build.
        #
        # Run whitespace only — style and analyzer formatters trigger IDE0051 false
        # positives for XAML-bound event handlers (dotnet format analyzes C# in
        # isolation, without the XAML compiler's generated partial classes that
        # reference those methods). MSBuild with EnforceCodeStyleInBuild=true
        # (below) enforces all style and dead-code rules after XAML compilation,
        # where XAML references are visible.
        run: dotnet format whitespace bae-windows\bae-windows.csproj --verify-no-changes

      - name: Install uniffi-bindgen-cs
        run: cargo install --git https://github.com/NordSecurity/uniffi-bindgen-cs.git --tag 'v0.11.0+v0.31.0' uniffi-bindgen-cs --locked

      # Both editions include the desktop bridge surface used by the WinUI app.
      # The full feature set adds OAuth provider exports; baeium omits them.
      # Build and generate both binding dirs so stale functions cannot linger
      # across editions.
      - name: Build the Windows bridge (baeium, desktop)
        shell: bash
        run: BAE_BRIDGE_FEATURES=desktop BAE_BRIDGE_CSHARP_BINDINGS_DIR=bae-bridge/csharp-bindings-baeium ./bae-bridge/build-windows.sh

      - name: Confirm the bridge DLL was produced
        shell: pwsh
        run: |
          $dll = "target-windows\x86_64-pc-windows-msvc\debug\bae_bridge.dll"
          if (Test-Path $dll) { Write-Host "PASS: $dll ($((Get-Item $dll).Length) bytes)" }
          else { Write-Host "FAIL: $dll not found"; exit 1 }

      - name: Build the Windows bridge (full edition, OAuth + desktop)
        # Windows has no cloudkit (Apple-only); full here is OAuth plus desktop.
        shell: bash
        run: BAE_BRIDGE_FEATURES=oauth-providers,desktop BAE_BRIDGE_CSHARP_BINDINGS_DIR=bae-bridge/csharp-bindings-full ./bae-bridge/build-windows.sh

      - name: Set up MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Add Windows SDK bin to PATH (makepri for resources.pri)
        shell: pwsh
        run: |
          # makepri.exe ships in the Windows 10 SDK but isn't on PATH on the
          # runner; the csproj's GenerateResourcesPri target invokes it bare to
          # compile resources.pri for the unpackaged app. Add the newest SDK x64
          # bin so the bare `makepri` command resolves (otherwise exit 9009).
          $makepri = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\makepri.exe" -ErrorAction SilentlyContinue |
            Sort-Object FullName -Descending | Select-Object -First 1
          if (-not $makepri) { throw "makepri.exe not found in the Windows SDK" }
          Add-Content -Path $env:GITHUB_PATH -Value $makepri.Directory.FullName
          Write-Host "makepri on PATH: $($makepri.FullName)"

      - name: Build the WinUI app (baeium bindings)
        # Unpackaged WinUI 3 (C#/.NET) app, x64, framework-dependent. Proves the
        # native UI stack compiles. Built with MSBuild, not `dotnet build`:
        # the WinUI XAML markup compiler needs the full VS toolchain to resolve
        # project-local x:DataType types (dotnet build drops the LocalAssembly
        # parameter — WMC1509 — and fails type resolution). A non-zero exit fails.
        # EnforceCodeStyleInBuild=true makes IDE-level dead-code diagnostics
        # (IDE0051 unused private members, IDE0052 unread private members) fire
        # as build errors via .editorconfig severity settings.
        run: msbuild bae-windows\bae-windows.csproj /restore /p:Configuration=Debug /p:Platform=x64 /p:EnforceCodeStyleInBuild=true /p:BridgeBindingsDir=..\bae-bridge\csharp-bindings-baeium

      - name: Build the WinUI app (full bindings)
        run: msbuild bae-windows\bae-windows.csproj /restore /p:Configuration=Debug /p:Platform=x64 /p:EnforceCodeStyleInBuild=true /p:BridgeBindingsDir=..\bae-bridge\csharp-bindings-full

      - name: Clippy (baeium / default features)
        # The host clippy in ci.yml targets macOS, so lint the Windows target too.
        run: cargo clippy -p bae-core -p bae-bridge --target x86_64-pc-windows-msvc -- -D warnings

      - name: Clippy (full edition, OAuth + desktop)
        # Dead-code warnings are feature-specific, so lint the OAuth/desktop code
        # on the Windows target too. No cloudkit on Windows.
        run: cargo clippy -p bae-core -p bae-bridge --target x86_64-pc-windows-msvc --features oauth-providers,desktop -- -D warnings
