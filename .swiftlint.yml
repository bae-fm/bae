disabled_rules:
  - todo                   # TODOs are tracked in issues, not the linter
  - trailing_whitespace    # covered by swift-format

opt_in_rules:
  - force_unwrapping       # flag optional force-unwraps (expr!)

excluded:
  - bae-macos/bae/bae/bae_bridge.swift
  - bae-ios/bae/bae/bae_bridge.swift
  - .build
  - .claude
